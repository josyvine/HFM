<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <!-- Video Display Area -->
    <VideoView
        android:id="@+id/video_view_full"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/footer_bar_video_viewer"
        android:layout_below="@+id/top_bar_video_viewer"
        android:layout_centerInParent="true" />

    <!-- Top Bar -->
    <RelativeLayout
        android:id="@+id/top_bar_video_viewer"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="#80000000">

        <TextView
            android:id="@+id/file_name_video_viewer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:layout_marginStart="16dp"
            android:layout_toStartOf="@+id/delete_button_video_viewer"
            android:ellipsize="middle"
            android:singleLine="true"
            android:text="Video File Name"
            android:textColor="@android:color/white"
            android:textSize="18sp" />

        <ImageButton
            android:id="@+id/delete_button_video_viewer"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_centerVertical="true"
            android:layout_toStartOf="@+id/close_button_video_viewer"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="Delete Video"
            android:src="@android:drawable/ic_menu_delete" />

        <ImageButton
            android:id="@+id/close_button_video_viewer"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="Close Video Viewer"
            android:src="@android:drawable/ic_menu_close_clear_cancel" />
    </RelativeLayout>

    <!-- Navigation Buttons -->
    <ImageButton
        android:id="@+id/prev_button_video_viewer"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginStart="16dp"
        android:background="#80000000"
        android:src="@android:drawable/ic_media_previous"
        android:contentDescription="Previous Video"
        android:scaleType="fitCenter" />

    <ImageButton
        android:id="@+id/next_button_video_viewer"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="16dp"
        android:background="#80000000"
        android:src="@android:drawable/ic_media_next"
        android:contentDescription="Next Video"
        android:scaleType="fitCenter" />

    <!-- NEW FOOTER -->
    <RelativeLayout
        android:id="@+id/footer_bar_video_viewer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="#80000000"
        android:padding="8dp">

        <LinearLayout
            android:id="@+id/footer_buttons_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/open_with_button_video"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:contentDescription="Open With"
                android:src="@android:drawable/ic_menu_set_as" />

            <ImageButton
                android:id="@+id/share_button_video"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:contentDescription="Share"
                android:src="@android:drawable/ic_menu_share" />

        </LinearLayout>

        <!-- DELETION PROGRESS UI (Initially Hidden) -->
        <RelativeLayout
            android:id="@+id/deletion_progress_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignTop="@id/footer_buttons_layout"
            android:layout_alignBottom="@id/footer_buttons_layout"
            android:visibility="gone">

            <ProgressBar
                android:id="@+id/deletion_progress_bar"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_centerVertical="true"
                android:layout_marginEnd="8dp"
                android:layout_toStartOf="@+id/deletion_progress_text"
                android:indeterminate="true" />

            <TextView
                android:id="@+id/deletion_progress_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:text="Deleting..."
                android:textColor="@android:color/white" />
        </RelativeLayout>

    </RelativeLayout>

</RelativeLayout>

